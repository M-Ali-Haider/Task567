import { ColorRing } from "react-loader-spinner";

const Loader = () => {
  return (
    <div className="mt-10">
      <ColorRing width={40} height={40} colors={[]} />
    </div>
  );
};

export default Loader;
